<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BodyFit Premium Core – AI Engineering Workspace</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts (Inter) -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Inter', 'sans-serif'] },
                    colors: {
                        brand: {
                            bg: '#090a0f',
                            surface: '#121420',
                            card: '#1a1d30',
                            border: '#262b45',
                            text: '#f8fafc',
                            muted: '#94a3b8',
                            accent: '#3b82f6',
                            accentHover: '#2563eb',
                            success: '#10b981'
                        }
                    },
                    boxShadow: {
                        'neon': '0 0 20px rgba(59, 130, 246, 0.15)',
                        'neon-strong': '0 0 30px rgba(59, 130, 246, 0.4)'
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background-color: #090a0f;
            color: #f8fafc;
            font-family: 'Inter', sans-serif;
        }
        /* Custom safe scrollbar styling */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: #090a0f; }
        ::-webkit-scrollbar-thumb { background: #262b45; border-radius: 999px; }
        ::-webkit-scrollbar-thumb:hover { background: #3b82f6; }
        
        .hidden-view { display: none !important; }
        .glass-panel {
            background: linear-gradient(135deg, rgba(18, 20, 32, 0.7) 0%, rgba(26, 29, 48, 0.6) 100%);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
        }
    </style>
</head>
<body class="antialiased h-screen w-screen overflow-hidden select-none">

    <!-- USER SIGN-UP & CREATION AUTH MODAL -->
    <div id="auth-modal" class="fixed inset-0 z-[200] flex items-center justify-center bg-black/80 backdrop-blur-xl p-4">
        <div class="glass-panel max-w-sm w-full p-8 rounded-3xl border border-brand-border text-center shadow-neon relative transform transition-all duration-300">
            <div class="w-12 h-12 rounded-2xl bg-gradient-to-tr from-brand-accent to-purple-600 text-white flex items-center justify-center mx-auto mb-4 font-black text-xl tracking-wider shadow-neon-strong">B</div>
            <h3 class="text-2xl font-extrabold tracking-tight text-brand-text mb-2">Create Security Account</h3>
            <p class="text-xs text-brand-muted font-medium mb-6">Initialize your encrypted local workspace container profiles.</p>
            
            <form onsubmit="handleAuthSubmit(event)" class="space-y-4 text-left">
                <div>
                    <label class="text-[10px] font-bold uppercase text-brand-muted tracking-widest block mb-1.5">Secure Email Axis</label>
                    <input type="email" required id="auth-email" placeholder="name@domain.com" class="w-full bg-brand-bg/60 border border-brand-border rounded-xl px-4 py-3 text-xs font-semibold text-brand-text placeholder:text-brand-muted/40 focus:outline-none focus:border-brand-accent focus:ring-1 focus:ring-brand-accent transition-all">
                </div>
                <div>
                    <label class="text-[10px] font-bold uppercase text-brand-muted tracking-widest block mb-1.5">Master Token Password</label>
                    <input type="password" required id="auth-password" placeholder="••••••••" class="w-full bg-brand-bg/60 border border-brand-border rounded-xl px-4 py-3 text-xs font-semibold text-brand-text placeholder:text-brand-muted/40 focus:outline-none focus:border-brand-accent focus:ring-1 focus:ring-brand-accent transition-all">
                </div>
                <button type="submit" class="w-full py-3.5 rounded-xl bg-brand-accent hover:bg-brand-accentHover text-brand-text font-bold text-xs uppercase tracking-widest transition-all duration-200 mt-4 shadow-md active:scale-[0.98]">Sign Up & Initialize</button>
            </form>
        </div>
    </div>

    <!-- TRANSACTION TIER PRICING MODAL -->
    <div id="payment-modal" class="fixed inset-0 z-[100] flex items-center justify-center bg-black/70 backdrop-blur-md hidden-view p-4 animate-fadeIn">
        <div class="glass-panel max-w-md w-full p-8 rounded-3xl border border-brand-border shadow-2xl relative overflow-y-auto max-h-[90vh]">
            <button onclick="closePaymentModal()" class="absolute top-5 right-5 text-brand-muted hover:text-brand-text text-2xl transition-colors">&times;</button>
            <h3 class="text-xl font-black text-brand-text mb-2 text-center tracking-tight">Upgrade High-Compute Arrays</h3>
            <p class="text-xs text-brand-muted text-center mb-6">Unlock deep matrix configurations & unrestricted predictive parsing loops.</p>
            
            <div class="grid gap-4 mb-6">
                <div class="bg-brand-card/60 p-4 rounded-2xl border border-brand-border flex justify-between items-center transition-all hover:border-brand-accent/40">
                    <div>
                        <h4 class="text-xs font-extrabold text-brand-text uppercase tracking-wider">Pro Monthly Plan</h4>
                        <p class="text-[11px] text-brand-muted mt-0.5">Custom structural macro metric profiles.</p>
                    </div>
                    <button onclick="triggerQrPayload('Pro Monthly', '350')" class="px-4 py-2 bg-brand-accent hover:bg-brand-accentHover text-brand-text font-bold text-xs rounded-xl shadow-md transition-all">₹350</button>
                </div>
                <div class="bg-brand-card/60 p-4 rounded-2xl border border-brand-border flex justify-between items-center transition-all hover:border-purple-500/40">
                    <div>
                        <h4 class="text-xs font-extrabold text-purple-400 uppercase tracking-wider">Elite Monthly Plan</h4>
                        <p class="text-[11px] text-brand-muted mt-0.5">Predictive telemetry neural analytics maps.</p>
                    </div>
                    <button onclick="triggerQrPayload('Elite Monthly', '1200')" class="px-4 py-2 bg-purple-600 hover:bg-purple-700 text-brand-text font-bold text-xs rounded-xl shadow-md transition-all">₹1200</button>
                </div>
                <div class="bg-brand-card/60 p-4 rounded-2xl border-2 border-brand-accent relative flex justify-between items-center shadow-neon">
                    <span class="absolute -top-2.5 left-4 px-2 py-0.5 bg-gradient-to-r from-red-500 to-amber-500 text-brand-text text-[8px] font-black uppercase rounded tracking-widest">Maximum Compute</span>
                    <div>
                        <h4 class="text-xs font-black text-brand-text uppercase tracking-wider">Elite Quarter (3 Months)</h4>
                        <p class="text-[11px] text-brand-muted mt-0.5">Absolute infrastructure system priority allocation.</p>
                    </div>
                    <button onclick="triggerQrPayload('Elite Quarter', '2500')" class="px-4 py-2 bg-gradient-to-r from-brand-accent to-purple-600 text-brand-text font-black text-xs rounded-xl shadow-md transition-all">₹2500</button>
                </div>
            </div>

            <!-- Dynamic QR Workspace Area -->
            <div id="modal-qr-frame" class="hidden-view text-center border-t border-brand-border pt-6 mt-4">
                <p class="text-xs font-bold text-brand-text mb-1" id="target-tier-title">Plan Variant Target</p>
                <p class="text-lg font-mono font-black text-brand-accent mb-4" id="target-tier-price">₹0</p>
                <div class="bg-white p-4 rounded-2xl inline-block mb-4 shadow-xl">
                    <!-- Default fallback graphic vector if empty -->
                    <img id="qr-code-img" src="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='150' height='150' viewBox='0 0 100 100'><rect width='100' height='100' fill='%23121420'/><text x='50' y='55' fill='%2394a3b8' font-size='5' font-family='sans-serif' font-weight='bold' text-anchor='middle'>QR Code Unset By Platform Admin</text></svg>" alt="Payment QR Code Node" class="w-40 h-40 mx-auto object-contain rounded-lg">
                </div>
                <p class="text-[10px] text-brand-muted font-medium leading-relaxed max-w-xs mx-auto mb-4">Transmit via secure authorization using any verified unified payment interface application layout.</p>
            </div>
            
            <button onclick="closePaymentModal()" class="w-full py-3 rounded-xl bg-brand-surface hover:bg-brand-card text-brand-muted hover:text-brand-text font-semibold text-xs uppercase tracking-widest border border-brand-border transition-all">Dismiss Pricing Terminal</button>
        </div>
    </div>

    <!-- MAIN CONSOLE LAYOUT FRAME -->
    <div id="app-view" class="h-screen w-screen flex bg-brand-bg overflow-hidden">
        
        <!-- SIDEBAR CONTAINER -->
        <aside class="w-76 h-full border-r border-brand-border flex flex-col justify-between p-5 bg-brand-surface/40 z-30 overflow-y-auto shrink-0 glass-panel">
            <div class="space-y-6">
                <div class="flex items-center justify-between px-1">
                    <div class="flex items-center space-x-2.5">
                        <div class="w-8 h-8 bg-gradient-to-tr from-brand-accent to-purple-600 rounded-xl text-brand-text font-black text-base flex items-center justify-center shadow-neon">B</div>
                        <span class="font-black text-sm text-brand-text tracking-wide uppercase">BodyFit Alpha</span>
                    </div>
                    <span id="tier-badge" class="text-[9px] bg-brand-card border border-brand-border text-brand-accent font-black px-2.5 py-1 rounded-lg uppercase tracking-wider shadow-inner">Base Plan</span>
                </div>
                
                <nav class="space-y-1.5 text-xs font-medium text-brand-text">
                    <div class="text-[10px] font-bold uppercase tracking-widest text-brand-muted/60 px-2.5 mb-2">Primary Array</div>
                    <button onclick="switchWorkspaceTab('chat-tab')" id="btn-chat-tab" class="w-full py-3 rounded-xl bg-brand-accent text-brand-text border border-brand-accent/30 flex items-center space-x-3 px-4 shadow-neon transition-all">
                        <span class="text-sm">⚡</span> <span class="font-bold tracking-wide">Core AI Conversational Mesh</span>
                    </button>
                    
                    <div class="text-[10px] font-bold uppercase tracking-widest text-brand-muted/60 px-2.5 pt-5 mb-2">Nutritional Processing</div>
                    <button onclick="switchWorkspaceTab('diet-tab', 'fridge')" id="btn-diet-fridge" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>📸</span> <span class="text-brand-muted font-semibold group-hover:text-brand-text">Fridge & Grocery Vision</span>
                    </button>
                    <button onclick="switchWorkspaceTab('diet-tab', 'recipe')" id="btn-diet-recipe" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>🥗</span> <span class="text-brand-muted font-semibold">Healthy Meal Matrix Builder</span>
                    </button>
                    <button onclick="switchWorkspaceTab('diet-tab', 'loss')" id="btn-diet-loss" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>📉</span> <span class="text-brand-muted font-semibold">Caloric Deficit Blueprint</span>
                    </button>
                    <button onclick="switchWorkspaceTab('diet-tab', 'gain')" id="btn-diet-gain" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>📈</span> <span class="text-brand-muted font-semibold">Hypertrophic Mass System</span>
                    </button>
                    <button onclick="switchWorkspaceTab('diet-tab', 'height')" id="btn-diet-height" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>📏</span> <span class="text-brand-muted font-semibold">Height Optimization Analytics</span>
                    </button>
                    
                    <div class="text-[10px] font-bold uppercase tracking-widest text-brand-muted/60 px-2.5 pt-5 mb-2">Fitness Synthesis</div>
                    <button onclick="switchWorkspaceTab('workouts-tab', 'home')" id="btn-workout-home" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>🏡</span> <span class="text-brand-muted font-semibold">Home Calisthenics Loops</span>
                    </button>
                    <button onclick="switchWorkspaceTab('workouts-tab', 'gym')" id="btn-workout-gym" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>🏋️</span> <span class="text-brand-muted font-semibold">Gym Kinetic Load Splits</span>
                    </button>
                    <button onclick="switchWorkspaceTab('workouts-tab', 'abs')" id="btn-workout-abs" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>🔥</span> <span class="text-brand-muted font-semibold">Core & Ab Sculpt Mapping</span>
                    </button>
                    
                    <div class="text-[10px] font-bold uppercase tracking-widest text-brand-muted/60 px-2.5 pt-5 mb-2">System Telemetry</div>
                    <button onclick="switchWorkspaceTab('trackers-tab', 'water')" id="btn-tracker-water" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>💧</span> <span class="text-brand-muted font-semibold">Volumetric Hydration</span>
                    </button>
                    <button onclick="switchWorkspaceTab('trackers-tab', 'sleep')" id="btn-tracker-sleep" class="w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all">
                        <span>😴</span> <span class="text-brand-muted font-semibold">Circadian Sleep Tracking</span>
                    </button>
                    
                    <div class="text-[10px] font-bold uppercase tracking-widest text-brand-muted/60 px-2.5 pt-5 mb-2">Platform Root</div>
                    <button onclick="switchWorkspaceTab('settings-tab')" id="btn-settings-tab" class="w-full py-3 rounded-xl border border-dashed border-brand-accent/40 text-brand-accent flex items-center space-x-3 px-4 text-left font-bold bg-brand-accent/5 transition-all hover:bg-brand-accent/10">
                        <span>🛡️</span> <span class="font-extrabold tracking-wide uppercase text-[11px]">Owner Administration Hub</span>
                    </button>
                </nav>
            </div>
            
            <div id="auth-status-panel" class="border-t border-brand-border pt-4 text-[11px] font-semibold text-brand-muted">
                System Mode: Unauthenticated
            </div>
        </aside>

        <!-- MAIN EXEC PANEL CONTAINER -->
        <main class="flex-1 h-full flex flex-col bg-brand-bg overflow-hidden relative">
            
            <!-- HEADER ARCHITECTURE WITH TOP RIGHT UPGRADE ACCESS -->
            <div class="h-16 border-b border-brand-border px-8 flex justify-between items-center text-xs font-semibold text-brand-muted bg-brand-surface/20 shrink-0 backdrop-blur-md z-10">
                <div>Environment Terminal Instance: <span class="font-mono text-brand-accent font-black tracking-wider text-xs">bodyfit.com/workspace</span></div>
                <div class="flex items-center space-x-4">
                    <!-- ACTION INTERFACE FOR PREMIUM TIER TRANSITION -->
                    <button onclick="openUpgradeModal()" class="bg-gradient-to-r from-brand-accent to-purple-600 hover:from-blue-500 hover:to-purple-500 text-brand-text font-black text-xs px-5 py-2.5 rounded-xl shadow-neon transition transform active:scale-95 duration-150">Upgrade Runtime Plan 💎</button>
                    <button onclick="resetAuthSession()" class="hover:text-red-400 border border-brand-border px-4 py-2.5 rounded-xl shadow-md transition bg-brand-card/40 text-[11px]">Purge Secure Cache</button>
                </div>
            </div>

            <!-- CORE CHAT WORKSPACE FRAME (ALWAYS THE BASE VISIBLE ELEMENT) -->
            <section id="chat-tab" class="flex-1 flex flex-col h-full overflow-hidden bg-brand-bg relative">
                
                <!-- Chat Window Space Container Stream -->
                <div class="flex-1 overflow-y-auto p-8 space-y-6 w-full max-w-4xl mx-auto" id="chatContainer">
                    <div class="flex space-x-4 items-start max-w-3xl">
                        <div class="w-8 h-8 rounded-xl bg-gradient-to-tr from-brand-accent to-purple-600 text-white flex items-center justify-center text-xs font-black shrink-0 shadow-neon">AI</div>
                        <div class="bg-brand-surface border border-brand-border/60 px-6 py-4 rounded-2xl rounded-tl-none text-sm font-medium leading-relaxed text-brand-text shadow-xl">
                            System layer operational across decentralized local parameters. Navigate to the <span class="text-brand-accent font-bold underline cursor-pointer" onclick="switchWorkspaceTab('settings-tab')">Owner Administration Hub</span> in the sidebar to configure your private access credentials.
                        </div>
                    </div>
                </div>

                <!-- STICKY FIXED CHAT INPUT HUD ARCHITECTURE -->
                <div class="p-6 bg-gradient-to-t from-brand-bg via-brand-bg to-transparent border-t border-brand-border/60 shrink-0 z-10">
                    <div class="max-w-3xl mx-auto flex items-center space-x-3 bg-brand-surface border border-brand-border rounded-2xl p-2 shadow-2xl focus-within:border-brand-accent/60 focus-within:shadow-neon transition-all duration-300">
                        <button onclick="toggleVoiceInput()" id="voice-btn" class="p-3.5 rounded-xl bg-brand-card border border-brand-border text-base hover:bg-brand-border transition-colors shadow-inner text-brand-text" title="Voice System Capture">🎙️</button>
                        <input type="text" id="chat-user-input" onkeydown="if(event.key==='Enter') executeLiveChatQuery()" placeholder="Initialize natural processing neural query or nutrition string lines..." class="flex-1 bg-transparent px-3 py-3 text-sm font-semibold focus:outline-none text-brand-text placeholder:text-brand-muted/30">
                        <button onclick="executeLiveChatQuery()" class="bg-brand-accent hover:bg-brand-accentHover text-brand-text font-bold px-6 py-3.5 rounded-xl text-xs uppercase tracking-widest transition-all shadow-md active:scale-[0.97]">Transmit</button>
                    </div>
                </div>
            </section>

            <!-- NUTRITIONAL WORKSPACE MODULE PANELS -->
            <section id="diet-tab" class="flex-1 overflow-y-auto p-8 max-w-4xl mx-auto w-full space-y-6 hidden-view">
                
                <!-- FRIDGE SECTION INTERFACE CONTAINER -->
                <div id="info-diet-fridge" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <div class="flex justify-between items-center">
                        <h3 class="text-lg font-bold text-brand-text flex items-center space-x-3"><span>📸</span> <span>Computer Vision Refrigerator Inventory Scanner</span></h3>
                        <span class="text-[10px] bg-brand-accent/10 border border-brand-accent/20 text-brand-accent font-black tracking-widest px-3 py-1 rounded-lg uppercase">Neural Logic</span>
                    </div>
                    <p class="text-xs text-brand-muted leading-relaxed font-medium">Deconstruct and map complex items into processing blocks to configure instant menus.</p>
                    <div class="border border-dashed border-brand-border p-8 rounded-2xl text-center bg-brand-bg/50 relative hover:bg-brand-card/20 transition-all cursor-pointer group">
                        <input type="file" accept="image/*" class="absolute inset-0 opacity-0 cursor-pointer" onchange="simulateFridgeVision(event)">
                        <div class="text-xs text-brand-muted font-semibold group-hover:text-brand-accent transition-colors">📷 Transmit Refrigerator Matrix Snapshot Data (Gallery / Camera Roll)</div>
                    </div>
                    <div id="fridge-recipe-output" class="hidden-view space-y-4 pt-4">
                        <div class="text-[10px] font-black text-brand-accent border-b border-brand-border pb-2 uppercase tracking-widest">=== PROCESSED RECIPE MAPS ===</div>
                        <div class="bg-brand-card/40 border border-brand-border p-5 rounded-2xl text-xs space-y-2">
                            <div class="text-sm font-bold text-brand-text">1. High-Density Bioavailability Protein Scramble</div>
                            <p class="text-brand-muted leading-relaxed">Synthesizes targeted tracked metrics utilizing detected farm eggs, fresh spinach foliage, and localized vegetable matter arrays.</p>
                        </div>
                    </div>
                </div>

                <!-- HEALTHY MEAL MATRIX BUILDER -->
                <div id="info-diet-recipe" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-lg font-bold text-brand-text flex items-center space-x-3"><span>🥗</span> <span>Healthy Meal Matrix Builder</span></h3>
                    <p class="text-xs text-brand-muted leading-relaxed">Dynamic processing matrices mapping macronutrient configurations across clean target structural pathways.</p>
                    <div class="bg-brand-card/40 border border-brand-border p-6 rounded-2xl text-xs text-brand-muted">
                        No custom meal profiles compiled yet. Engage the Core AI Conversational Mesh with specific macro boundaries to output data.
                    </div>
                </div>

                <!-- PRECISION DEFICIT BLUEPRINT -->
                <div id="info-diet-loss" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-lg font-bold text-red-400 flex items-center space-x-3"><span>📉</span> <span>Precision Caloric Deficit Blueprint</span></h3>
                    <p class="text-xs text-brand-muted leading-relaxed">Basal Metabolic Rate configurations calculating targeted lean adipose tissue attenuation pacing parameters.</p>
                </div>

                <!-- HYPERTROPHIC MASS TRACKS -->
                <div id="info-diet-gain" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-lg font-bold text-emerald-400 flex items-center space-x-3"><span>📈</span> <span>Hypertrophic Mass Gain Framework</span></h3>
                    <p class="text-xs text-brand-muted leading-relaxed">Calculated positive system balance boundaries to maximize muscular glycogen distribution stores without accelerating unnecessary lipid loading matrices.</p>
                </div>

                <!-- HEIGHT GROWTH SEGMENT CONTAINER MAP -->
                <div id="info-diet-height" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <div class="flex justify-between items-center">
                        <h3 class="text-lg font-bold text-purple-400 flex items-center space-x-3"><span>📏</span> <span>Height Optimization & Skeletal Support</span></h3>
                        <span class="text-[10px] bg-purple-500/10 border border-purple-500/20 text-purple-400 font-black tracking-widest px-3 py-1 rounded-lg uppercase">Biomechanics</span>
                    </div>
                    <p class="text-xs text-brand-muted leading-relaxed">Decompress spinal paths, reinforce trace micronutrient mineral loading frameworks, and calculate posturing vectors.</p>
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 text-xs">
                        <div class="bg-brand-card p-5 rounded-2xl border border-brand-border">
                            <div class="font-bold text-brand-text mb-1">📐 Intervertebral Spine Architecture</div>
                            <p class="text-brand-muted leading-relaxed text-[11px]">System layout configuration tracking decompression models and precise physical stretching vectors to ensure perfect posture tracking matrices.</p>
                        </div>
                        <div class="bg-brand-card p-5 rounded-2xl border border-brand-border">
                            <div class="font-bold text-brand-text mb-1">🦴 Micronutrient Calcification Profiles</div>
                            <p class="text-brand-muted leading-relaxed text-[11px]">Optimizing precise bioavailable Calcium, D3, K2, and foundational structural system mineral tracking maps to optimize dense skeletal infrastructure integrity.</p>
                        </div>
                    </div>
                </div>
            </section>

            <!-- FITNESS SYNTHESIS MODULE HUB -->
            <section id="workouts-tab" class="flex-1 overflow-y-auto p-8 max-w-4xl mx-auto w-full space-y-6 hidden-view">
                
                <!-- HOME CALISTHENICS MODULE -->
                <div id="info-workout-home" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-lg font-bold text-brand-text flex items-center space-x-3"><span>🏡</span> <span>Home Calisthenics Routine Arrays</span></h3>
                    <p class="text-xs text-brand-muted leading-relaxed">High-density bodyweight resistance load processing circuits omitting peripheral mechanical asset requirements.</p>
                </div>

                <!-- GYM KINETIC SPLITS -->
                <div id="info-workout-gym" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-lg font-bold text-brand-text flex items-center space-x-3"><span>🏋️</span> <span>Gym Kinetic Compound Loading Splits</span></h3>
                    <p class="text-xs text-brand-muted leading-relaxed">Targeted hypertrophy loading tracking frameworks across multi-joint mechanical infrastructure parameters.</p>
                </div>

                <!-- CORE AND AB SCULPT -->
                <div id="info-workout-abs" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-lg font-bold text-brand-text flex items-center space-x-3"><span>🔥</span> <span>Abdominal Stabilization Core Layouts</span></h3>
                    <p class="text-xs text-brand-muted leading-relaxed">Deep transverse encapsulation metrics designed to isolate secondary visceral architecture loops and secure lumbar structural integrity paths.</p>
                </div>
            </section>

            <!-- TELEMETRY STREAMS MONITOR MAPS -->
            <section id="trackers-tab" class="flex-1 overflow-y-auto p-8 max-w-4xl mx-auto w-full space-y-6 hidden-view">
                
                <!-- VOLUMETRIC HYDRATION TRACKER NODE -->
                <div id="info-tracker-water" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-base font-bold text-brand-text">💧 Volumetric Hydration Optimization Matrix</h3>
                    <p class="text-xs text-brand-muted">Track system hydration fluid requirements dynamically.</p>
                    <div class="text-4xl font-mono font-black text-brand-text my-4 tracking-wider"><span id="water-count" class="text-brand-accent">0.00</span> <span class="text-sm font-sans font-bold text-brand-muted">/ 3.50 Liters</span></div>
                    <button onclick="addWater()" class="text-xs px-5 py-3 bg-brand-accent hover:bg-brand-accentHover text-brand-text font-bold rounded-xl transition-all shadow-neon active:scale-95">Log 250ml Allocation Fluid Packet</button>
                </div>

                <!-- CIRCADIAN SLEEP NODE -->
                <div id="info-tracker-sleep" class="bg-brand-surface p-8 rounded-3xl border border-brand-border shadow-xl hidden-view space-y-4">
                    <h3 class="text-base font-bold text-brand-text">😴 Circadian REM / Sleep Recovery Node</h3>
                    <div class="text-3xl font-mono font-black text-brand-text my-2">7.50 Hours</div>
                    <div class="text-xs text-brand-success font-bold tracking-widest uppercase bg-brand-success/10 border border-brand-success/20 px-3 py-1 rounded-lg inline-block">Telemetry State: Restorative Execution Optimal</div>
                </div>
            </section>

            <!-- OWNER SECURE ADMINISTRATIVE GATEWAY HUB -->
            <section id="settings-tab" class="flex-1 overflow-y-auto p-8 max-w-2xl mx-auto w-full space-y-6 hidden-view">
                <div class="flex justify-between items-center border-b border-brand-border pb-4">
                    <div class="space-y-1">
                        <h2 class="text-xl font-black text-brand-text tracking-wide uppercase">Owner Administrative Hub</h2>
                        <p class="text-xs text-brand-muted">Root-level parameter key caching engine controls.</p>
                    </div>
                    <span class="text-[10px] font-mono bg-red-900/20 border border-red-500/30 text-red-400 px-3 py-1 rounded-lg font-black tracking-widest uppercase">Root Config</span>
                </div>

                <!-- LIVE CRITICAL FIX: TOKENS STORED DIRECTLY IN ADMIN AREA ONLY -->
                <div class="bg-brand-surface p-6 rounded-2xl border border-brand-border space-y-4 shadow-xl">
                    <div class="text-xs font-black text-brand-text uppercase tracking-widest flex items-center space-x-2">
                        <span class="text-brand-accent">🔑</span> <span>OpenRouter Endpoint API Security Token</span>
                    </div>
                    <p class="text-[11px] text-brand-muted leading-relaxed">Input your private token array down below. This token securely caches in your individual client browser storage instance, surviving page refreshes automatically.</p>
                    
                    <div class="space-y-3">
                        <div>
                            <label class="text-[10px] font-bold uppercase tracking-wider text-brand-muted block mb-1">Model Selection Node</label>
                            <select id="api-model-select" class="bg-brand-bg border border-brand-border rounded-xl px-4 py-3 text-xs font-bold focus:outline-none focus:border-brand-accent text-brand-text w-full transition-colors">
                                <option value="google/gemini-2.5-flash">OpenRouter ID: Google Gemini 1.5 Flash</option>
                                <option value="meta-llama/llama-3-8b-instruct">OpenRouter ID: Meta Llama 3</option>
                            </select>
                        </div>
                        <div>
                            <label class="text-[10px] font-bold uppercase tracking-wider text-brand-muted block mb-1">API Key Signature Pass</label>
                            <div class="flex gap-2">
                                <input type="password" id="api-token-input" placeholder="sk-or-v1-..." class="bg-brand-bg border border-brand-border rounded-xl px-4 py-3 text-xs font-semibold w-full focus:outline-none focus:border-brand-accent text-brand-text tracking-wider placeholder:text-brand-muted/20 transition-all">
                                <button onclick="verifyAndConnectApi()" class="bg-brand-accent hover:bg-brand-accentHover text-brand-text font-bold text-xs px-5 py-3 rounded-xl shadow-md transition-all whitespace-nowrap active:scale-95">Commit Token</button>
                            </div>
                        </div>
                        <div id="token-status-indicator" class="text-[10px] text-red-400 font-bold mt-1 tracking-wide">
                            Status Array: No Security Token Committed To Cache Memory.
                        </div>
                    </div>
                </div>

                <!-- LIVE CRITICAL FIX: GPAY MERCHANT QR ENCODE ENVELOPE BASE -->
                <div class="bg-brand-surface p-6 rounded-2xl border border-brand-border space-y-4 shadow-xl">
                    <div class="text-xs font-black text-brand-text uppercase tracking-widest flex items-center space-x-2">
                        <span class="text-brand-accent">🔒</span> <span>Merchant Base GPay QR Target Vector</span>
                    </div>
                    <p class="text-[11px] text-brand-muted leading-relaxed">Upload your custom platform business generation matrix graphic vector down below to dynamically change the runtime billing modal interface across reload triggers.</p>
                    
                    <div class="border-2 border-dashed border-brand-border p-8 rounded-2xl text-center bg-brand-bg/50 relative hover:bg-brand-card/30 transition-all cursor-pointer group">
                        <input type="file" id="owner-qr-uploader" accept="image/*" class="absolute inset-0 opacity-0 cursor-pointer" onchange="ownerUpdateSystemQr(event)">
                        <div class="text-xs text-brand-muted font-bold group-hover:text-brand-accent transition-colors">📲 Commit System Universal QR Code Graphic Asset from Files/Gallery</div>
                    </div>
                    <div id="admin-qr-preview-container" class="hidden-view pt-2 text-center">
                        <span class="text-[10px] text-brand-success font-black uppercase tracking-wider block mb-2">Active System Node Asset Cached Below:</span>
                        <img id="admin-qr-preview" src="" alt="Admin Preview" class="w-24 h-24 mx-auto object-contain bg-white p-2 rounded-xl border border-brand-border">
                    </div>
                </div>
            </section>
        </main>
    </div>

    <!-- SCRIPT INFRASTRUCTURE & PERSISTENCE CODES -->
    <script>
        // High-level senior architectural state system tracking data arrays accurately
        let appState = { 
            isAuthenticated: false, 
            userEmail: '', 
            userTier: 'Base Plan', 
            savedQrData: null,
            apiToken: '',
            selectedModel: 'google/gemini-2.5-flash',
            chatHistory: [] 
        };
        let waterVolume = 0.0;

        // LIFE CYCLE RUNTIME HANDLER: Runs at boot parameters to instantly fetch data maps
        window.addEventListener('DOMContentLoaded', () => {
            const savedState = localStorage.getItem('bodyfit_secure_v3');
            if (savedState) { 
                appState = JSON.parse(savedState); 
                
                // Set authenticated states if previously committed
                if (appState.isAuthenticated) {
                    hideAuthModal();
                    hydrateSavedChatHistory();
                }
                
                // PERSISTENCE BLOCK FIX: Auto restore API token properties inside text fields safely
                if (appState.apiToken) {
                    document.getElementById('api-token-input').value = appState.apiToken;
                    const indicator = document.getElementById('token-status-indicator');
                    indicator.innerText = "Status Array: Token Active & Secured Globally In Local Storage Cache ✅";
                    indicator.className = "text-[10px] text-emerald-400 font-bold mt-1 tracking-wide";
                }
                
                // PERSISTENCE BLOCK FIX: Hydrate dynamic custom target QR parameters across interface layers
                if (appState.savedQrData) { 
                    renderQrFromCache(); 
                }
                
                if (appState.selectedModel) {
                    document.getElementById('api-model-select').value = appState.selectedModel;
                }
                
                document.getElementById('tier-badge').innerText = appState.userTier;
            }
        });

        function saveState() { 
            localStorage.setItem('bodyfit_secure_v3', JSON.stringify(appState)); 
        }

        /* AUTHORIZATION PIPELINE CONTROL MAPS */
        function handleAuthSubmit(e) {
            e.preventDefault();
            const email = document.getElementById('auth-email').value.trim();
            if(!email) return;
            appState.isAuthenticated = true;
            appState.userEmail = email;
            saveState();
            hideAuthModal();
        }

        function hideAuthModal() {
            document.getElementById('auth-modal').classList.add('hidden-view');
            document.getElementById('auth-status-panel').innerHTML = `Active Core Key Identity:<br><span class="text-brand-accent font-black text-xs font-mono tracking-wide">${appState.userEmail}</span>`;
        }

        function resetAuthSession() {
            localStorage.removeItem('bodyfit_secure_v3');
            location.reload();
        }

        /* CONSOLE & WORKSPACE VIEW ROUTING MATRIX */
        function openUpgradeModal() {
            document.getElementById('payment-modal').classList.remove('hidden-view');
        }

        function closePaymentModal() { 
            document.getElementById('payment-modal').classList.add('hidden-view'); 
        }

        function triggerQrPayload(tierName, rawPrice) {
            document.getElementById('target-tier-title').innerText = `Selected Profile Block: ${tierName}`;
            document.getElementById('target-tier-price').innerText = `₹${rawPrice}`;
            document.getElementById('modal-qr-frame').classList.remove('hidden-view');
        }

        // COMMIT KEY METHOD: Secure and encapsulate into local states
        function verifyAndConnectApi() {
            const token = document.getElementById('api-token-input').value.trim();
            const selectedModel = document.getElementById('api-model-select').value;
            
            if(!token) {
                alert("Matrix Fault: Input valid OpenRouter security signature block first.");
                return;
            }
            
            // Lock values inside local structure instances safely
            appState.apiToken = token;
            appState.selectedModel = selectedModel;
            saveState();
            
            const indicator = document.getElementById('token-status-indicator');
            indicator.innerText = "Status Array: Token Active & Secured Globally In Local Storage Cache ✅";
            indicator.className = "text-[10px] text-emerald-400 font-bold mt-1 tracking-wide";
            
            alert("Administrative Matrix Success: Security credentials successfully stored inside local operational cache pipelines.");
        }

        // SYSTEM QR ASSIGNMENT CONTROL BLOCK: Updates view maps instantly from reader arrays
        function ownerUpdateSystemQr(event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    appState.savedQrData = e.target.result;
                    saveState();
                    renderQrFromCache();
                    alert("Administrative Matrix Success: Platform universal payment destination asset compiled & updated successfully.");
                }
                reader.readAsDataURL(file);
            }
        }

        function renderQrFromCache() {
            if(appState.savedQrData) {
                // Instantly swap view sources across billing frames and admin panels
                document.getElementById('qr-code-img').src = appState.savedQrData;
                document.getElementById('admin-qr-preview').src = appState.savedQrData;
                document.getElementById('admin-qr-preview-container').classList.remove('hidden-view');
            }
        }

        // SENIOR TAB ROUTER LOGIC: Fully maps specific keys without dropping visual display blocks
        function switchWorkspaceTab(targetTabId, targetSectionKey = null) {
            // Disengage visibility fields across all global elements
            ['chat-tab', 'diet-tab', 'workouts-tab', 'trackers-tab', 'settings-tab'].forEach(tab => {
                document.getElementById(tab).classList.add('hidden-view');
            });
            
            // Pop rendering visible limits onto targeted segments
            document.getElementById(targetTabId).classList.remove('hidden-view');
            
            // Reset active item layout visual indicators down across all side buttons safely
            document.querySelectorAll('aside nav button').forEach(btn => {
                btn.className = "w-full py-2.5 rounded-xl hover:bg-brand-card/40 border border-transparent hover:border-brand-border/60 flex items-center space-x-3 px-4 text-left transition-all text-brand-muted text-xs font-semibold";
            });
            
            // If internal parameter strings are specified (like height tracking maps) activate layout screens explicitly
            if (targetSectionKey) {
                let targetPrefix = targetTabId === 'diet-tab' ? 'diet' : (targetTabId === 'workouts-tab' ? 'workout' : 'tracker');
                
                // Synchronize active highlights safely onto selection vectors
                document.getElementById(`btn-${targetPrefix}-${targetSectionKey}`).className = "w-full py-2.5 rounded-xl bg-brand-card border border-brand-border flex items-center space-x-3 px-4 text-left text-brand-text font-bold shadow-neon";
                
                // Hide peripheral adjacent sibling layouts inside target view blocks cleanly
                if(targetTabId === 'diet-tab') {
                    ['fridge', 'recipe', 'loss', 'gain', 'height'].forEach(k => document.getElementById(`info-diet-${k}`).classList.add('hidden-view'));
                } else if(targetTabId === 'workouts-tab') {
                    ['home', 'gym', 'abs'].forEach(k => document.getElementById(`info-workout-${k}`).classList.add('hidden-view'));
                } else if(targetTabId === 'trackers-tab') {
                    ['water', 'sleep'].forEach(k => document.getElementById(`info-tracker-${k}`).classList.add('hidden-view'));
                }
                
                // Make selected specific information array view window fully operational
                document.getElementById(`info-${targetPrefix}-${targetSectionKey}`).classList.remove('hidden-view');
            } else {
                // If it is an isolated component stream without sub keys, bind interface blocks directly
                document.getElementById(`btn-${targetTabId}`).className = "w-full py-3 rounded-xl bg-brand-accent text-brand-text border border-brand-accent/30 flex items-center space-x-3 px-4 shadow-neon transition-all text-xs font-bold tracking-wide";
            }
        }

        /* METRIC HANDLERS */
        function addWater() { 
            waterVolume += 0.25; 
            document.getElementById('water-count').innerText = waterVolume.toFixed(2); 
        }
        
        function simulateFridgeVision(event) { 
            if(event.target.files.length > 0) { 
                document.getElementById('fridge-recipe-output').classList.remove('hidden-view'); 
            } 
        }

        function toggleVoiceInput() {
            window.SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
            if(!window.SpeechRecognition) { alert("Speech Recognition matrix pipeline unavailable on this user agent."); return; }
            const recognition = new SpeechRecognition();
            const voiceBtn = document.getElementById('voice-btn');
            voiceBtn.innerText = "🛑";
            recognition.addEventListener('result', e => {
                document.getElementById('chat-user-input').value = e.results[0][0].transcript;
            });
            recognition.addEventListener('end', () => voiceBtn.innerText = "🎙️");
            recognition.start();
        }

        /* CONVERSATION INTERACTION PIPELINES */
        function hydrateSavedChatHistory() {
            const container = document.getElementById('chatContainer');
            appState.chatHistory.forEach(msg => {
                appendChatBubble(msg.text, msg.isUser, false);
            });
        }

        function appendChatBubble(text, isUser, shouldSave = true) {
            const container = document.getElementById('chatContainer');
            const bubble = document.createElement('div');
            
            if (isUser) {
                bubble.className = "flex space-x-4 items-start justify-end max-w-3xl ml-auto w-full animate-fadeIn";
                bubble.innerHTML = `<div class="bg-gradient-to-tr from-brand-accent to-blue-600 text-brand-text px-6 py-4 rounded-2xl rounded-tr-none text-sm font-semibold leading-relaxed max-w-xl shadow-xl border border-brand-accent/20">${text}</div>`;
            } else {
                bubble.className = "flex space-x-4 items-start max-w-3xl w-full animate-fadeIn";
                bubble.innerHTML = `<div class="w-8 h-8 rounded-xl bg-gradient-to-tr from-brand-accent to-purple-600 text-white flex items-center justify-center text-xs font-black shrink-0 shadow-neon">AI</div><div class="bg-brand-surface border border-brand-border/60 px-6 py-4 rounded-2xl rounded-tl-none text-sm font-medium leading-relaxed text-brand-text shadow-xl">${text}</div>`;
            }
            
            container.appendChild(bubble);
            container.scrollTop = container.scrollHeight;
            
            if (shouldSave) {
                appState.chatHistory.push({ text: text, isUser: isUser });
                saveState();
            }
        }

        async function executeLiveChatQuery() {
            const inputField = document.getElementById('chat-user-input');
            const userText = inputField.value.trim();
            
            // Dynamically load token states directly from memory arrays safely
            const token = appState.apiToken;
            const model = appState.selectedModel;
            
            if(!userText) return;
            if(!token) { 
                alert("Execution Halted: No OpenRouter Token active in current local security context blocks. Please route onto the Owner Administration Hub link in your sidebar settings menu panel layout, input token, and commit."); 
                return; 
            }

            appendChatBubble(userText, true);
            inputField.value = '';

            const container = document.getElementById('chatContainer');
            const aiBubble = document.createElement('div');
            aiBubble.className = "flex space-x-4 items-start max-w-3xl w-full";
            aiBubble.innerHTML = `<div class="w-8 h-8 rounded-xl bg-gradient-to-tr from-brand-accent to-purple-600 text-white flex items-center justify-center text-xs font-black shrink-0 shadow-neon">AI</div><div class="bg-brand-surface border border-brand-border/60 px-6 py-4 rounded-2xl rounded-tl-none text-sm font-medium leading-relaxed text-brand-text shadow-xl" id="live-ai-node">Parsing secure cloud parameters...</div>`;
            container.appendChild(aiBubble);
            container.scrollTop = container.scrollHeight;
            const liveAiNode = document.getElementById('live-ai-node');

            try {
                const response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
                    method: "POST",
                    headers: { 
                        "Authorization": `Bearer ${token}`, 
                        "Content-Type": "application/json",
                        "HTTP-Referer": "https://bodyfit.com",
                        "X-Title": "BodyFit Studio Workstation"
                    },
                    body: JSON.stringify({ model: model, messages: [{ role: "user", content: userText }] })
                });
                const data = await response.json();
                const aiReply = data.choices?.[0]?.message?.content || "Data system sequence structure execution failure.";
                liveAiNode.innerText = aiReply;
                
                appState.chatHistory.push({ text: aiReply, isUser: false });
                saveState();
            } catch (err) {
                liveAiNode.innerText = "Network data matrix pipeline sync fault encountered. Check key properties.";
            }
            liveAiNode.removeAttribute('id');
            container.scrollTop = container.scrollHeight;
        }
    </script>
</body>
</html>

